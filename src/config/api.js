// API Configuration
const API_BASE_URL = 'http://localhost:5000/api';

export const API_ENDPOINTS = {
  projects: `${API_BASE_URL}/projects`,
  contact: `${API_BASE_URL}/contact`,
  resume: `${API_BASE_URL}/download/resume`,
};

export default API_BASE_URL; 